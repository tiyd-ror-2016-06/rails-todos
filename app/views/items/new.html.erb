<h1>Add a New Item</h1>

<%# form_for [@list, @item] do %>
<%= form_for @item, url: list_items_path(@list) do |f| %>
  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.label :due_date %>
  <%= f.date_field :due_date %>
  <!-- <div class="<%= @item.errors[:description].any? ? 'invalid' : '' %>">
    <label for="description">Description</label>
    <input
      type="text"
      name="description"
      placeholder="What do you want to do?"
      value="<%= @item.description %>"
    >
    <% if @item.errors[:description].any? %>
      <ul>
        <% @item.errors[:description].each do |err| %>
          <li><%= err %></li>
        <% end %>
      </ul>
    <% end %>
  </div>

  <div class="<%= @item.errors[:due_date].any? ? 'invalid' : '' %>">
    <label for="due_date">Due</label>
    <input
      type="date"
      name="due_date"
      placeholder="Due"
      value="<%= @item.due_date %>"
    >
    <% if @item.errors[:due_date].any? %>
      <ul>
        <% @item.errors[:due_date].each do |err| %>
          <li><%= err %></li>
        <% end %>
      </ul>
    <% end %>
  </div> -->

  <button>Save</button>
<% end %>
